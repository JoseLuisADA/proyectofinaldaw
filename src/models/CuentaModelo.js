class Cuenta {
    constructor(cuentaId, nombre, avatar, rol) {
      this.cuentaId = cuentaId;
      this.nombre = nombre;
      this.avatar = avatar;
      this.rol = rol;
    }
  }
  
export default Cuenta;
  