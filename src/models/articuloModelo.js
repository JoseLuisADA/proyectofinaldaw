class Articulo {
  constructor(cuentaId, titulo, contenido, fechaCreacion) {
    this.cuentaId = cuentaId;
    this.titulo = titulo;
    this.contenido = contenido;
    this.fechaCreacion = fechaCreacion;
  }
}

export default Articulo;
